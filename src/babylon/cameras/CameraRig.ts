export class CameraRig {
  public updateTracking(targetX: number, targetY: number): void {
    // Clamps focal length and centers action view
  }

  public shake(amplitude: number): void {
    // Evaluates camera offset spring solver
  }
}
