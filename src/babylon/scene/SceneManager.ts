export class SceneManager {
  private canvas: HTMLCanvasElement;

  constructor(canvas: HTMLCanvasElement) {
    this.canvas = canvas;
  }

  public initialize(): void {
    // Instantiates WebGL / WebGPU scene
  }

  public render(): void {
    // Primary camera view render step
  }
}
